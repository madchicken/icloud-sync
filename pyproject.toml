[project]
name = "virtualicloud"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyicloud @ file:///Users/madchicken/Projects/pyicloud",
    "watchdog>=4.0",
    "keyring>=25.0",
    "rumps>=0.4",
]

[project.scripts]
icloud-sync = "icloud_sync.cli:main"
icloud-sync-tray = "icloud_sync.tray_app:main"
